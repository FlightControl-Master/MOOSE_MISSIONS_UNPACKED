# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: windows-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Checkout unpacked demo missions
      uses: actions/checkout@v2.3.4 
      with:
        path: unpacked
   #
    - name: Checkout packed demo missions
      uses: actions/checkout@v2.3.4
      with:
        repository: FlightControl-Master/MOOSE_MISSIONS
        path: packed
  #
    - name: Checkout Moose Include
      uses: actions/checkout@v2.3.4
      with:
        repository: FlightControl-Master/MOOSE_INCLUDE
        path: include
      
    #build
    - name: build demo missions
      run: | 
        echo "Hello ${{ github.event.inputs.name }}"
        dir
        dir ..


